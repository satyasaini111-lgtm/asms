server:
  port: 8080

spring:
  application:
    name: api-gateway
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: ${KEYCLOAK_ISSUER_URI:http://localhost:8180/realms/asms}
          jwk-set-uri: ${KEYCLOAK_JWK_URI:http://localhost:8180/realms/asms/protocol/openid-connect/certs}
  cloud:
    gateway:
      default-filters:
        - AddResponseHeader=X-Content-Type-Options, nosniff
        - AddResponseHeader=X-Frame-Options, DENY
        - AddResponseHeader=Strict-Transport-Security, max-age=31536000; includeSubDomains
        - RemoveResponseHeader=Server
      routes:
        - id: user-service
          uri: http://user-service:8081
          predicates:
            - Path=/api/v1/users/**
        - id: amenity-service
          uri: http://amenity-service:8083
          predicates:
            - Path=/api/v1/amenities/**,/api/v1/bookings/**
        - id: support-service
          uri: http://support-service:8084
          predicates:
            - Path=/api/v1/tickets/**
        - id: visitor-service
          uri: http://visitor-service:8085
          predicates:
            - Path=/api/v1/visitors/**
        - id: payment-service
          uri: http://payment-service:8086
          predicates:
            - Path=/api/v1/payments/**
        - id: billing-service
          uri: http://billing-service:8087
          predicates:
            - Path=/api/v1/invoices/**
        - id: workflow-service
          uri: http://workflow-service:8088
          predicates:
            - Path=/api/v1/workflows/**
        - id: helpbot-service
          uri: http://helpbot-service:8090
          predicates:
            - Path=/api/v1/helpbot/**
  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus,metrics
  metrics:
    tags:
      application: ${spring.application.name}
  tracing:
    sampling:
      probability: 1.0

logging:
  level:
    org.springframework.cloud.gateway: DEBUG
    org.springframework.security: WARN
